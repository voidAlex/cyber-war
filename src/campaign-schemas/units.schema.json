{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "campaign-schemas/units.schema.json",
  "title": "CampaignUnits",
  "description": "初始单位部署（编制+初始坐标+战力/人员/燃料/弹药/士气/疲劳）。严格校验供 ajv 导入包。",
  "type": "array",
  "minItems": 1,
  "items": {
    "type": "object",
    "properties": {
      "id": { "type": "string", "minLength": 1 },
      "factionId": { "type": "string", "minLength": 1 },
      "type": {
        "type": "string",
        "enum": ["infantry", "armor", "artillery", "recon", "fortress", "support", "air", "naval", "missile", "ew"]
      },
      "coord": {
        "type": "object",
        "properties": {
          "col": { "type": "integer", "minimum": 0 },
          "row": { "type": "integer", "minimum": 0 }
        },
        "required": ["col", "row"],
        "additionalProperties": false
      },
      "strength": { "type": "number", "minimum": 0, "maximum": 100 },
      "personnel": { "type": "integer", "minimum": 0 },
      "maxPersonnel": { "type": "integer", "minimum": 1 },
      "fuel": { "type": "number", "minimum": 0, "maximum": 100 },
      "ammo": { "type": "number", "minimum": 0, "maximum": 100 },
      "morale": { "type": "number", "minimum": 0, "maximum": 100 },
      "fatigue": { "type": "number", "minimum": 0, "maximum": 100 },
      "status": {
        "type": "array",
        "items": {
          "type": "string",
          "enum": ["engaged", "suppressed", "pinned", "retreating", "low_supply", "decoy"]
        },
        "uniqueItems": true
      },
      "equipment": {
        "type": "array",
        "description": "第 5 批：装备槽列表（影响火力加成）。缺省视为无装备加成。",
        "items": {
          "type": "object",
          "properties": {
            "type": { "type": "string", "minLength": 1, "description": "装备类型标识（如 rifle/howitzer/missile）" },
            "count": { "type": "integer", "minimum": 0 },
            "quality": { "type": "number", "minimum": 0, "maximum": 1 }
          },
          "required": ["type", "count", "quality"],
          "additionalProperties": false
        }
      },
      "ewCapability": {
        "type": "object",
        "description": "T2 第 2 批：电子战能力（可选）。由 electronic-warfare.applyEWEffects 消费。",
        "properties": {
          "jammingRange": { "type": "number", "minimum": 0, "maximum": 20, "description": "干扰范围（曼哈顿距离），范围内敌方单位 intel level 降低" },
          "detectionBoost": { "type": "number", "minimum": 0, "maximum": 3, "description": "对己方侦察的情报加成（+N level，封顶 L3）" },
          "stealthReduction": { "type": "number", "minimum": 0, "maximum": 1, "description": "降低敌方对该单位的侦察等级倍率（0=完全隐身，1=无加成）" }
        },
        "required": ["jammingRange", "detectionBoost", "stealthReduction"],
        "additionalProperties": false
      }
    },
    "required": ["id", "factionId", "type", "coord", "strength", "personnel", "maxPersonnel", "fuel", "ammo", "morale", "fatigue"],
    "additionalProperties": false
  }
}
